let cli = require('mycli')
// 引用的都是lib 下的文件

// const {program} = require('commander')

// console.log('hello')

// // program.parse(['-f','./tesst.js'])
// // program.parse(process.argv)
// // program.parse(['node', '-f','./tesst.js'])

// program.version('0.0.111') // 注册执行函数 匹配命令
//     .parse(process.argv); // 传入命令行参数执行
// // program.version('0.0.111','-v').parse(process.argv);